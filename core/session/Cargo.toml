[package]
name = "session"
version = "0.1.0"
edition = "2021"

[dependencies]
ast = { path = "../ast" }
runtime = { path = "../runtime" }
compiler = { path = "../compiler" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
