[package]
name = "compiler"
version = "0.1.0"
edition = "2021"

[dependencies]
ast = { path = "../ast" }
runtime = { path = "../runtime" }
thiserror = "1"

[dev-dependencies]
parser = { path = "../parser" }
tempfile = "3"
